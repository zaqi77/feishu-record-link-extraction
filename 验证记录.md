# 验证记录

日期：2026-09-22。验证对象：本目录源码与构建产物。

## 已通过

- Node 内置测试：9 项通过。覆盖跨 200 条分页、顺序与去重、零值分页 token、异常分页中止、取消、4 路并发、单条失败、链接格式检查、超时、CSV 中文/引号/公式文本处理。
- 浏览器模拟宿主：选中 3 条、视图 257 条、全表 451 条；CSV 行数与最后一条记录核对；结果只预览前 100 条，导出不截断。
- 交互：剪贴板成功/权限拒绝回退、空选区、非 Grid 视图提示、单条无权限、停止任务、提取中切表/变更选区、视图筛选变更检测、复制异步回调与新结果隔离、记录变更清空结果。
- 界面：410px、360px、浅色及深色；窄屏无横向溢出；浏览器无未捕获异常。截图在 `artifacts/`，明确标注模拟数据。
- TypeScript 检查与 Vite 生产构建通过；`dist/index.html` 使用相对资源路径；`package.json` 指定 `output: dist`。

## 验证边界

- 浏览器测试替换了 SDK 宿主响应，用于验证插件逻辑与界面，未读取真实业务表。
- 官方 SDK 的实际类型已经参与编译；分页与链接方法已对照官方 API 文档及用户指南 revision 453。
- 尚未在用户真实 Base 中安装。仍需对照右键「复制记录链接」验证打开同一记录，以及 Wiki、权限受限、被当前视图过滤的记录场景。
- 源码和静态产物已准备；尚未公开部署或提交插件中心审核。
- 构建有 SDK 主包超过 500 KB 的体积提示，构建本身成功；本次沿用官方 SDK，未改动其内部实现。
